# workClaw server configuration
# Environment variables (see .env.example) override values in this file.

[server]
host = "0.0.0.0"
port = 3000
database_url = "sqlite:herness.db"
jwt_secret = "dev-secret-change-in-production"
rust_log = "info"
skills_dir = "./skills"
