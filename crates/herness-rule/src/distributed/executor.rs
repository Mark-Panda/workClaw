/// Placeholder for distributed rule execution.
pub struct DistributedExecutor;

impl DistributedExecutor {
    pub fn new() -> Self {
        Self
    }
}

impl Default for DistributedExecutor {
    fn default() -> Self {
        Self::new()
    }
}
